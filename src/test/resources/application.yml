spring:
  security:
    oauth2:
      client:
        provider:
          oidc:
            issuer-uri: https://dev-931599.okta.com/oauth2/default
        registration:
          oidc:
            provider: oidc
            client-id: test
            client-secret: secret